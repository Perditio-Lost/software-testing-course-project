<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CourseMapper">

    <select id="selectAll" resultType="com.example.entity.Course">
        select course.*, admin.name as adminName from `course`
        left join admin on course.admin_id = admin.id
        <where>
            <if test="name != null"> and LOWER(course.name) like LOWER(concat('%', #{name}, '%'))</if>
            <if test="adminId != null"> and admin_id = #{adminId}</if>
        </where>
    </select>

    <delete id="deleteById">
        delete from `course`
        where id = #{id}
    </delete>

    <!-- insert into course (username, password, ...) values ('course', 'course', ...) -->
    <insert id="insert" parameterType="com.example.entity.Course" useGeneratedKeys="true" keyProperty="id">
        insert into `course`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null">name,</if>
            <if test="score != null">score,</if>
            <if test="adminId != null">admin_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="name != null">#{name},</if>
            <if test="score != null">#{score},</if>
            <if test="adminId != null">#{adminId},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Course">
        update `course`
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="score != null">
                score = #{score},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>